import 'package:flutter/material.dart';

class Archive extends StatelessWidget {
  String title = "Archiv";

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    return Center(child: Text("Noch keine Archivdaten vorhanden"));
  }
}
